spring:
  profiles:
    active: dev
    #    active: staging

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: inventory-service
server:
  port: 0
  error:
    include-message: always